% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_logs.R
\name{rc_export_logs}
\alias{rc_export_logs}
\title{Export REDCap Project Logs}
\usage{
rc_export_logs(log_type = "", begin_time = "", end_time = "")
}
\arguments{
\item{log_type}{Optional character string specifying the type of log events to export.
Valid values include \code{"record"}, \code{"manage"}, \code{"file"}, and others supported by REDCap.
Defaults to \code{""} (all log types).}

\item{begin_time}{Optional character string specifying the start date/time for filtering logs.
Must be in the format \code{"YYYY-MM-DD HH:MM:SS"}. Defaults to \code{""} (no lower bound).}

\item{end_time}{Optional character string specifying the end date/time for filtering logs.
Must be in the format \code{"YYYY-MM-DD HH:MM:SS"}. Defaults to \code{""} (no upper bound).}
}
\value{
A data frame (or list) of log entries returned by the REDCap API, parsed from JSON.
}
\description{
Retrieves project log events from the REDCap API.
}
\details{
This function calls the REDCap API with the \code{content = 'log'} parameter
and returns the project logs in JSON format as an R object.
The function requires that the API URL and token be stored in environment
variables \code{RC_API_URL} and \code{RC_API_TOKEN}.

The function requires that two environment variables be set:
\itemize{
\item \code{RC_API_URL}: the base URL of the REDCap API endpoint.
\item \code{RC_API_TOKEN}: a valid API token for the project.
}

If the API request fails, the function stops with an error message.
}
\examples{
\dontrun{
# Export all logs
logs <- rc_export_logs()

# Export only record logs within a date range
logs <- rc_export_logs(
  log_type   = "record",
  begin_time = "2025-01-01 00:00:00",
  end_time   = "2025-01-31 23:59:59"
)
}

}
\seealso{
\href{https://redcap.vanderbilt.edu/api/}{REDCap API Documentation}
}
