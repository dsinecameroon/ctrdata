% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_records_between.R
\name{rc_export_records_between}
\alias{rc_export_records_between}
\title{Export REDCap Records Within a Date Range}
\usage{
rc_export_records_between(record_id = "record_id", start_date, end_date)
}
\arguments{
\item{record_id}{A string indicating the name of the record ID field. Default is \code{'record_id'}.}

\item{start_date}{The start date (inclusive) in \code{"YYYY-MM-DD"} format.}

\item{end_date}{The end date (inclusive) in \code{"YYYY-MM-DD"} format.}
}
\value{
A data frame of all REDCap records whose survey timestamps fall within the given date range.
}
\description{
Extracts all REDCap records whose survey timestamp falls between the specified start and end dates.
Uses environment variables \code{RC_API_URL} and \code{RC_API_TOKEN} set via \code{rc_config()}. It relies on a helper
function \code{rc_timestamp_var()} to detect timestamp fields and assumes a \code{date_survey} column is created
or standardized via \code{ctr_dates_reconciliation()}.
}
\details{
This function performs three steps:
\enumerate{
\item Retrieves record IDs and timestamp fields using \code{rc_timestamp_var()}.
\item Filters records between \code{start_date} and \code{end_date} based on \code{date_survey}.
\item Fetches the full record data for only the matching records.
}

Environment variables \code{RC_API_URL} and \code{RC_API_TOKEN} must be set before calling this function.
Timestamps must be parsable as \code{Date} objects.
}
\examples{
rc_config("https://redcap.your.org/api/", "XYZ123TOKEN")
rc_export_records_between(start_date = "2024-01-01", end_date = "2024-03-31")

}
