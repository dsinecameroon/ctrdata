% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_single_record.R
\name{rc_export_single_record}
\alias{rc_export_single_record}
\title{Export a Single REDCap Record by Matching Field Value}
\usage{
rc_export_single_record(field_name, field_value)
}
\arguments{
\item{field_name}{A character string specifying the name of the field to search in (e.g., \code{"email"} or \code{"participant_id"}).}

\item{field_value}{A character string specifying the value to search for in the given field.}
}
\value{
A data frame containing the full record that matches the given field value.
Returns \code{NULL} if no match is found.
}
\description{
Searches REDCap records for a value in a specific field and returns the full record that matches.
This function uses the API URL and token from environment variables set via \code{rc_config()}.
}
\details{
The function performs the following steps:
\enumerate{
\item Retrieves only the \code{record_id} and the specified \code{field_name} from all records.
\item Filters to find a record where the field matches \code{field_value} using a partial match (\code{grepl}).
\item Extracts and returns the full record for the matching \code{record_id}.
}

The REDCap API URL and token must be set beforehand using \code{rc_config(api_url, api_token)}.
}
\examples{
rc_config("https://redcap.your.org/api/", "XYZ123TOKEN")
rc_export_single_record(field_name = "email", field_value = "john.doe@example.com")

}
