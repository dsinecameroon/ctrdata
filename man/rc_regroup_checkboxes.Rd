% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regroup_checkboxes.R
\name{rc_regroup_checkboxes}
\alias{rc_regroup_checkboxes}
\title{Regroup Checkbox Dummies in a Full REDCap Dataset}
\usage{
rc_regroup_checkboxes(data)
}
\arguments{
\item{data}{A data frame exported from REDCap (flat format).}
}
\value{
The same data frame with additional grouped columns for each checkbox
field, placed before their corresponding dummy columns.
}
\description{
Scans a REDCap dataset for checkbox dummy variables (columns containing \verb{___})
and creates a grouped column for each checkbox field. The grouped column contains
the selected raw codes joined by commas and is relocated to appear immediately
before the related dummy columns.
}
\details{
\itemize{
\item Checkbox dummies are detected by the \verb{___} separator in column names.
\item For each unique base field, the function combines selected codes into a single
comma-separated string (e.g., \code{"0,2,5"}).
\item The grouped column is inserted immediately before the dummy variables in the
output dataset.
}
}
\examples{
\dontrun{
full_data <- rc_export_full_dataset()
regrouped <- rc_regroup_checkboxes(full_data)
}

}
