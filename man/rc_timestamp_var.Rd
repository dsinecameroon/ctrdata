% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_timestamp_variables.R
\name{rc_timestamp_var}
\alias{rc_timestamp_var}
\title{Extract Timestamp Field Names from a REDCap Project}
\usage{
rc_timestamp_var(rec_id = 1)
}
\arguments{
\item{rec_id}{A single record ID (usually a number or string) used to pull one record from REDCap.
Default is \code{1}. The record must exist; otherwise, the function may fail.}
}
\value{
A character vector of field names that end with \code{"timestamp"}.
}
\description{
Identifies all field names that end in \code{"timestamp"} in a REDCap project by inspecting a sample record.
This is useful for dynamically selecting timestamp fields without hardcoding them.
Requires that the REDCap API environment variables are set via \code{rc_config()}.
}
\details{
\itemize{
\item The function sends a REDCap API request to export a single record using the given \code{rec_id}.
\item It inspects the field names in the response and returns those that match the pattern \code{"timestamp$"}.
\item The function relies on environment variables \code{RC_API_URL} and \code{RC_API_TOKEN} set using \code{rc_config()}.
}
}
\examples{
\dontrun{
rc_config("https://redcap.your.org/api/", "XYZ123TOKEN")
rc_timestamp_var()
}

}
